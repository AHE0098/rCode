# Input folder (`in/`)

Put optional source files here.

Current harness behavior:
- If `in/` is empty, the run script generates example deck data.
- Future versions can read CSV/JSON files from this folder.

Suggested future files:
- `cards.csv`
- `deck_rules.json`
